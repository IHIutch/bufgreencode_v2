# Everything under /_astro/ is content-hashed by the Astro build, so it can be
# cached forever. Without this, Workers static assets defaults every response to
# `max-age=0, must-revalidate`.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
